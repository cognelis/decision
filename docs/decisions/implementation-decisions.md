# Decision 实施决策

本文件记录用户授权 Codex 在实施期间代做的关键技术与产品决策。它只记录会影响边界、兼容性或维护成本的选择，不记录普通编码细节。

| ID | 日期 | 决策 | 理由 | 重新评估条件 |
| --- | --- | --- | --- | --- |
| IMP-001 | 2026-07-24 | 使用 npm workspaces | 本机已安装 npm，避免额外引入包管理器 | workspace 工具限制构建或发布 |
| IMP-002 | 2026-07-24 | 在当前任务中顺序实施 | 用户授权代做决策，但未要求启用并行代理 | 用户明确要求并行代理 |
| IMP-003 | 2026-07-24 | 固定 Electron 主版本并提交 lockfile | 保证 Electron 内置 Node 与 `node:sqlite` 行为可复现 | 计划升级 Electron |
| IMP-004 | 2026-07-24 | 使用隔离 worktree 和功能分支 | 保留已确认规格的主分支，便于逐步提交与回退 | 功能分支完成并通过验收 |
| IMP-005 | 2026-07-24 | 直接适配 TypeScript 7 的相对 `paths`，不降级 | 新项目无需延续已移除的 `baseUrl`，遵循当前工具链可以减少后续迁移 | 构建工具无法解析同一别名 |
| IMP-006 | 2026-07-24 | SQLite 使用内置 `node:sqlite` 与 FTS5，并限制在 App 主进程 | 避免跨平台原生模块重编译；同步 API 在唯一写入者中边界清晰 | 数据量导致主进程出现可测量卡顿 |
| IMP-007 | 2026-07-24 | 本地通道使用原生 `node:http`、随机 loopback 端口和单次启动令牌 | 不引入 Web 框架；边界小、跨平台、可直接做请求体与认证限制 | 需要跨用户或远程通信 |
| IMP-008 | 2026-07-24 | bridge 使用官方 MCP TypeScript SDK 1.29，并只解析版本化显式 Stop 标记 | 保持 Claude Code/Codex 契约一致，避免从普通自然语言中猜测决策 | MCP 主版本升级或 hooks 提供原生结构化决策事件 |
| IMP-009 | 2026-07-24 | hooks 合并 JSON；MCP 注册调用 Claude/Codex 官方 CLI | 避免重写 `~/.claude.json` 和 `config.toml` 的未知字段或注释；Codex 独立 `hooks.json` 是官方支持位置 | 官方 CLI 不再提供幂等 MCP 管理命令 |
| IMP-010 | 2026-07-24 | Electron 从设计时的 40 升到当前稳定版 43.2.0 | Electron 40 已结束支持；43.2.0 是当前稳定版本并包含 Node 24.18，可直接提供所需 `node:sqlite` | Electron 43 到达支持终止或出现阻断性回归 |
| IMP-011 | 2026-07-24 | 决策笔记文件名使用可读 ID 前缀加完整 ID 的 SHA-256 短指纹 | 仅截取 ID 前缀会让同前缀决策覆盖；稳定指纹兼顾可读性与防碰撞 | 迁移到内容寻址或 Obsidian 提供原生唯一文件标识 |
| IMP-012 | 2026-07-24 | 小岛态允许长选项省略，但为“推荐”文字保留固定空间 | 原生 420px 窗口实测中长标签会挤掉推荐标识；推荐不能只依赖颜色表达 | 小岛宽度或交互密度重新设计 |
| IMP-013 | 2026-07-24 | 应用数据目录固定使用产品名 `Decision` | Electron 曾按 npm 包名生成小写目录，和 bridge 的跨进程发现路径不一致；显式产品名和平台路径可消除隐式耦合 | 引入统一的平台路径包或安装器支持自定义数据目录 |
| IMP-014 | 2026-07-24 | 测试覆盖目录同时作为 Electron `userData`，并在单实例锁前设置 | 只隔离业务文件仍会与已安装 App 共用 Chromium 单实例锁；完整隔离才能并行验证真实安装包 | Electron 改变单实例锁或路径初始化语义 |
| IMP-015 | 2026-07-24 | 持久化失败时保留已完成决策并显式重试 | 用户已经输入的选择和理由不能因 vault 暂不可写而丢失，也不能静默推进队列 | 引入跨重启 outbox |
| IMP-016 | 2026-07-24 | SQLite 损坏文件先隔离，再以同目录临时库原子重建 | SQLite 是派生索引，恢复应优先保证 App 可启动、Markdown 不受影响和替换过程不暴露半成品 | 更换索引引擎或引入多进程读取 |
| IMP-017 | 2026-07-24 | MCP 取消使用幂等键贯通到队列 | 等待中的 HTTP 响应尚未返回 decision ID，幂等键是调用方已知且稳定的取消标识 | 协议增加立即确认与异步结果通道 |
| IMP-018 | 2026-07-24 | SQLite 主文件及 WAL/SHM 均强制为 `0600` | 派生索引仍包含决策问题与理由，不能因默认 umask 让同机其他用户读取 | 索引改为不含正文或迁移到系统密钥保护存储 |
| IMP-019 | 2026-08-02 | 第二阶段先交付独立决策库，渲染进程只接收展示字段 | 搜索与回看是结果回填、原则提炼和统计的共同入口；不暴露 Markdown 路径可缩小桌面界面权限边界 | 后续结果模型需要写回 Markdown |
| IMP-020 | 2026-08-02 | 后续结果直接写回原决策 Markdown，再同步可重建索引 | 保持 Markdown 唯一事实源，并让 Obsidian 外部编辑、索引重建与全文搜索继续使用同一数据链路 | 结果演进为多次带时间戳的观察记录 |
| IMP-021 | 2026-08-02 | 预期/实际复盘先采用用户确认的结构化结论，不复用当前语义分类网关伪装生成分析 | “优于预期”等结论会成为后续方法论提炼的事实标签；现有模型网关只实现受限语义分类，强行复用会产生不可信的自由分析 | 引入经过独立 Schema 校验、留痕和降级处理的通用结构化生成网关 |
| IMP-022 | 2026-08-02 | 方法论提炼使用独立的通用结构化生成契约，候选必须保留来源证据并由用户确认后才能采纳 | 语义分类与原则生成的输出边界不同；Schema 校验、逐后端留痕和人工确认可以防止未经验证的模型归纳成为正式方法论 | 累积足够人工确认样本后，引入可测量的自动分组、冲突检测或置信校准 |
| IMP-023 | 2026-08-02 | 第一版知识图谱作为已采纳原则的只读投影，不单独持久化图节点与边 | 项目、来源决策、人工复盘结果和原则之间已有可验证引用；运行时确定性构建能避免第二事实源，并保证候选不会提前进入正式知识关系 | 需要用户编辑关系、表达跨原则冲突，或图谱规模要求增量计算与专用查询引擎 |
| IMP-024 | 2026-08-02 | 技能与工作流先作为可审核的 Obsidian Markdown 草案，技能使用独立目录中的 `SKILL.md`，默认不自动安装客户端 | 保持 Markdown 唯一事实来源和人工确认边界；向 Codex、Claude Code 等客户端写入配置属于额外外部变更，未经确认不应执行，也不能让未验证的方法论直接影响后续任务 | 增加由用户明确触发的发布流程、版本管理、同名冲突处理和可回滚安装后重新评估 |
| IMP-025 | 2026-08-02 | 批量分析使用临时内存 DuckDB 导入可重建 SQLite 快照，不维护持久化 DuckDB 仓库 | DuckDB 适合跨决策列式聚合，但当前数据量不需要第二事实源；内存实例禁用外部访问并限制线程与内存，既落实第二阶段分析能力，也保持 Markdown/SQLite 的既有恢复边界 | 决策规模导致每次全量导入达到可测量延迟或内存压力，或需要历史分析快照与增量查询 |
| IMP-026 | 2026-08-06 | 复盘计划使用本地日历日期写入决策 Markdown，到期只进入首页收件箱 | 结果验证通常围绕“哪一天回看”而不是绝对时区时间；保留 Markdown 事实源并禁止到期事项主动唤醒窗口，可以补齐复盘闭环而不恢复侵入式交互 | 需要跨时区团队共享或精确到时刻的提醒 |
| IMP-027 | 2026-08-06 | 未配对普通文本候选由 Hook 每小时机会式巡检，超过 24 小时或格式损坏即删除 | 只在同会话再次消费时判断过期会让不再继续的会话正文长期残留；低频目录巡检能落实既有隐私承诺，又避免每次 Hook 全量扫描 | 候选规模增长到目录巡检产生可测量延迟，或引入常驻清理服务 |
| IMP-028 | 2026-08-06 | 方法论可信度由来源证据数量、跨项目覆盖和复盘结果一致性确定；文本相近与对立行动信号只触发人工复核 | 模型自报可信度不可验证；确定性质量投影可解释、可重算，并能在不自动合并或覆盖 Markdown 的前提下拦截缺失证据和提示重复风险 | 积累足够已确认的相近/冲突标签后，引入经过保留集评估的语义聚类或关系模型 |
| IMP-029 | 2026-08-06 | 已采纳技能与工作流都通过用户明确触发，以 Skill 包发布到 Codex 或 Claude Code；同名覆盖需要再次确认，发布历史与原文件快照保存在应用数据目录 | 两个客户端本机都采用目录化 `SKILL.md` 契约；统一发布形态可以保留实践资产的条件、步骤、检查与失败处理，同时避免自动写入和不可恢复覆盖 | 增加其它客户端适配、跨设备同步或批量发布前，先验证本机版本回滚与冲突提示的使用反馈 |
| IMP-030 | 2026-08-06 | 方法论提炼建议只对未被活动原则使用的完整复盘进行确定性分组，依据文本相似度、结果方向与项目覆盖度预选最多 5 条证据，不在后台调用模型 | 让用户能看见“现在有哪些证据可提炼”，同时避免把无关或方向相反的复盘交给模型强行归纳；建议是可解释的操作入口，不是自动方法论 | 积累用户调整建议组合的反馈后，再评估经过保留集验证的语义聚类，并保留手动选证据入口 |
| IMP-031 | 2026-08-06 | 实践资产的新鲜度由来源原则的存在性、采纳状态和更新时间确定性重算；来源更新只生成替换草案，采纳后回写原稳定标识，发布前再次校验 | 防止来源变化静默覆盖已采纳资产或客户端内容，同时保留已安装版本、发布历史和安全回滚；稳定标识避免同一实践产生多个客户端副本 | 需要对多版本来源差异做逐字段审阅，或需要跨设备协同更新时，引入显式版本图和三方合并 |
| IMP-032 | 2026-08-06 | 实践资产在 Markdown 中保存来源原则快照用于确定性逐字段比较；已采纳资产修改前的最近 20 个完整版本保存在 App 数据目录，恢复前再次建立检查点 | 来源快照需要随事实笔记移动，恢复数据则不应污染用户可见的 Obsidian 目录；有界完整快照比不可逆增量补丁更容易校验和恢复，且内部来源内容不会进入客户端 Skill 包 | 需要跨设备同步、长期审计或多人合并时，将有界本地恢复点升级为显式版本图并设计同步冲突协议 |
| IMP-033 | 2026-08-06 | 方法论冷启动同时支持复盘建议批量生成和本地 Markdown 导入；导入内容使用独立来源类型、默认低可信，0 条复盘证据时必须明确确认并以假设身份采纳 | 历史复盘可以高效转成候选，但外部文字不能被伪装成已验证证据；来源类型与质量状态分离后，旧笔记仍可迁移，导入候选也能继续人工编辑、去重和质量检查 | 未来支持证据关联编辑、长文多原则拆分或跨设备导入时，继续保留来源声明和人工确认边界，不自动提升可信度 |
| IMP-034 | 2026-08-06 | Markdown 长文只按可解释的同级编号或字段结构拆分，每文件最多 12 条、单次最多新增 60 条；导入候选可由用户关联 1 至 5 条完整复盘证据并确定性重算可信度 | 冷启动需要把已有知识转成可审核候选，但自动语义切分或自动匹配证据会掩盖来源错误；显式边界、人工选证据和内容不自动改写可以兼顾效率与可核对性 | 积累足够的拆分修正和证据关联反馈后，再评估经过保留集验证的语义切分与匹配建议 |
| IMP-035 | 2026-08-06 | 导入原则的证据发现只在主进程对最近最多 200 条完整复盘做有界确定性匹配，返回最多 5 条字段级解释；渲染层只展示、搜索和手动选择，不自动关联 | 让已有方法论更容易连接真实结果，同时避免浏览器加载核心运行时代码、模型猜测来源或推荐结果直接改变事实关系；匹配不可用时完整复盘列表仍可操作 | 积累用户采纳、跳过与搜索行为的脱敏反馈，并用保留集证明语义匹配显著优于当前可解释基线后再升级算法 |
| IMP-036 | 2026-08-06 | 原则间人工结论使用独立、规范化且对称的 Markdown 关系事实，每对原则只保留一个“重复、冲突或无关”结论及可选说明；关系操作不改写原则 | 把人工判断塞进任一原则会产生单向状态和重命名同步问题；独立事实可由任一端读取、修改和撤销，并让“无关”抑制误报、让已确认冲突在文本变化后继续可见，同时不把关系核对误当作自动合并 | 需要真正合并原则、多原则关系组、跨设备协同或完整关系历史时，引入显式版本图和可恢复的合并事务 |
| IMP-037 | 2026-08-06 | 未核对的原则关系在渲染层按规范化原则对去重、冲突优先排序，并通过单个连续比较队列人工处理；队列只复用既有逐对关系写入接口 | 关系数量增长后逐条进入详情会造成重复导航，新增第二种批量事实格式又会破坏“一对关系一个事实”的边界；按需读取全部方法论并连续调用既有原子操作可以提高效率，同时保持每次结论独立失败、可修改和可撤销 | 关系规模导致全量按需投影出现可测量延迟，或需要多人协作分派关系审核任务时，把队列下沉为有游标的主进程查询 |
| IMP-038 | 2026-08-06 | 知识图谱只投影双方都已采纳的“重复/冲突”人工关系，并继续排除“无关”和候选关系；搜索、筛选与成对聚焦只发生在渲染层 | 正式图谱不能把未确认候选或已判定无关的提示伪装成知识关系；直接复用独立 Markdown 关系事实和既有原则节点即可保持只读、可重建边界，而本地浏览状态没有必要形成新的持久化事实 | 需要自定义边、多原则关系组、图布局坐标持久化或图查询规模出现可测量瓶颈时，引入显式图编辑契约和专用索引 |
| IMP-039 | 2026-08-06 | 只有两条已采纳且人工确认为重复的原则可以建立人工合并草案；草案追溯两条来源和其已有复盘，不调用模型，也不自动处置来源原则或实践资产 | “重复”结论不等于已经决定删除哪条知识；把合并实现为新的可编辑候选并在采纳时重验来源与关系，可以形成可回退闭环，同时避免静默丢失历史、破坏已发布实践或把过期关系当作当前事实 | 需要归档来源、批量重新指向实践资产或支持多原则合并时，设计带预览、引用迁移和完整回滚的显式合并事务 |
| IMP-040 | 2026-08-06 | 已采纳合并原则通过显式整理向导逐项生成实践资产来源迁移草案；旧来源只有在没有活动引用后才标为可恢复的“已归档” | 自动批量重写会把一次知识整理扩大成多次模型调用和客户端内容变更；逐项草案、独立审核、发布状态不变和可恢复归档可以让影响范围可见，并保证旧知识不会在引用仍存在时消失 | 需要多原则合并、跨资产事务回滚或引用规模导致逐项操作不可接受时，设计带检查点与恢复日志的有界批处理事务 |
| IMP-041 | 2026-08-06 | 决策只允许人工关联最多 5 条实际采用的原则；新关联仅限已采纳原则，归档后保留历史关联；原则详情只投影采用后的复盘分布，不声称因果 | 模型猜测“用了哪条原则”会把推断写成事实，而单纯统计正向结果也会制造虚假的效果证明；把关系写入决策 Markdown、让 SQLite 可重建，并把结果明确标为相关分布，既能形成使用反馈闭环又保留人工确认和可审计边界 | 需要自动建议关联或量化原则效果时，先积累人工标注并设计控制混杂因素、保留集评估和明确可撤销确认的独立机制 |
| IMP-042 | 2026-08-06 | 原则采用验证对全部历史计算精确待结果/待复盘计数和下一条待处理决策，渲染层只接收最近 50 条可筛选明细；从原则跳转时按决策 ID 精确查询 | 用截断明细反推总数会在数据增长后产生错误提醒，而依赖决策库最近 200 条列表会让早期采用记录无法打开；主进程保留完整统计和有界展示、SQLite 提供严格校验的精确 ID 查询，可以同时保证反馈闭环、性能边界和导航确定性 | 采用记录超过当前内存快照与最近 50 条交互范围并出现可测量延迟时，将明细升级为主进程分页查询，同时保持总计数和下一条待处理入口稳定 |
| IMP-043 | 2026-08-06 | 补理由阶段只用本地确定性文本重合提示最多 3 条已采纳原则，用户明确点选后才随决策保存；不调用模型，也不把这个阶段包装成事前建议 | App 收到捕获事件时原生答案已经完成，自动宣称原则被采用会把推断写成事实；有解释的本地核对、最多 5 条持久化上限和跨重启恢复可以先闭合方法论使用反馈，同时保持答案与事实边界 | 客户端协议能够在答案提交前暂停并提供独立建议阶段，且事前推荐经过保留集评估与人工确认后，再扩展为真正的选择前支持 |
| IMP-044 | 2026-08-06 | 方法论冷启动进度由主进程对完整 SQLite 决策快照、全部原则和实践资产确定性聚合；少量候选时持续展示闭环状态与下一步，不用有界列表反推总数 | 原有获取说明只在 0 条候选时出现，出现第一条后就留下大片无意义空白；渲染层用最近 200 条列表统计还会在数据增长后产生错误进度。完整只读聚合能把真实内容来源和操作路径持续讲清楚，同时不增加模型调用或新的事实源 | 数据规模使按页读取全部方法论和实践资产出现可测量延迟，或需要跨设备团队进度时，引入专用聚合查询与同步边界 |
| IMP-045 | 2026-08-06 | 方法论 Markdown 导入改为主进程内存预检与一次性明确提交；完全重复不进入选择，同原则不同边界标为可能重复且默认不选，导入笔记只保留来源文件名与内容 SHA-256 | 选择文件后立即写入会隐藏长文拆分、字段缺失和重复判断，渲染进程接收原路径又会扩大权限边界；有时限的一次性批次让用户能逐条核对且不把文件内容或路径变成第二份持久化数据，提交时重验重复则避免预检期间状态变化导致重复写入 | 需要跨窗口恢复预检、大批量分页导入或内容级三方合并时，设计加密暂存、批次恢复和显式冲突处理协议 |
| IMP-046 | 2026-08-06 | 已采纳原则可以不调用模型直接建立人工技能或工作流草案；来源仅预填适用条件与失败边界，步骤和验收标准必须由用户填写，并复用现有候选、来源快照、采纳、版本和显式发布生命周期 | 把实践落地绑定到可用模型会让方法论在离线或用户希望自行编写时停在原则层；独立人工入口既保留事实来源与审核边界，也避免系统以“辅助”为名虚构执行内容 | 需要多人共同编写、模板复用或离线草稿自动保存时，在候选生命周期上增加作者、协作版本和显式模板来源，不改变采纳与发布边界 |
| IMP-047 | 2026-08-08 | 原则创建统一收口到“新建原则”，并把人工录入作为独立的 0 次模型调用来源；人工候选默认无证据、可稍后关联完整复盘，继续复用质量检查与采纳确认 | 分散的生成与导入按钮隐藏了内容的三种真实获取方式，也让没有历史复盘的用户无法直接沉淀判断；独立来源能同时解决冷启动和来源诚实性，避免把人工知识伪装成模型提炼或 Markdown 导入 | 需要多人作者身份、自动保存或模板化录入时，在人工来源上扩展作者和草稿版本，不自动建立证据或绕过候选审核 |
| IMP-048 | 2026-08-08 | 已采纳原则只能从明确采用它并完成复盘的新决策建立人工修订草案；应用时保留稳定原则编号、先保存完整恢复点，再更新内容与证据，并让下游实践资产确定性标记为来源已更新 | 直接编辑正式原则会丢失旧边界，也会让历史决策与已发布实践在无审核的情况下指向新含义；独立草案、显式差异确认、稳定编号和有界恢复点可以保留历史关联，同时让下游更新继续经过自己的草案与发布边界 | 需要模型辅助修订、多作者协作或超过最近 50 条采用明细选择证据时，在现有候选和版本生命周期上增加显式模式与分页查询，不允许自动应用或自动发布 |
| IMP-049 | 2026-08-08 | 复盘提炼建议作为方法论页的常驻素材收件箱；用户可用只保存建议哈希的私有本地状态搁置和恢复组合，已经生成或明确忽略的候选证据不再重新推荐 | 建议只在空状态出现会让已有方法论用户找不到新增复盘，无法跳过则会让同一组合长期占据入口；可恢复搁置能保持收件箱可管理，又不把浏览状态伪装成知识事实或保存复盘正文。忽略候选应代表这组证据已经人工处理，不能随后自动回流 | 需要跨设备同步素材处理状态或为新证据自动重开相近组合时，设计可合并的建议版本和显式重开原因，不自动生成或采纳原则 |
| IMP-050 | 2026-08-08 | 选择完整复盘证据后同时提供“人工整理 0 次”和“模型提炼 1 次”；人工路径保存真实来源、人工证据摘要和可重算可信度，但继续只建立候选 | 把证据驱动的原则创建绑定到可用生成模型，会让离线用户和希望亲自归纳的用户只能先建无证据原则再二次关联，既割裂又容易漏掉来源；在同一证据选择后明确分流可以保持调用成本可见，并让人工知识同样具备可审核证据链 | 需要协作编辑、自动保存或模型给出可选修订建议时，在人工草稿上增加显式版本和建议层，不允许模型静默覆盖用户填写内容 |
| IMP-051 | 2026-08-08 | 原则采用后的新增复盘集中进入人工复验收件箱；确认仍适用只在原则 Markdown 中保存有序复验游标，不改变内容更新时间、可信度或正文 | 把演进提示藏在单条原则详情会让大量原则无法持续维护，而用内容更新时间记录一次核对又会把下游实践资产错误标记为过期；独立游标可以让收件箱跨重启消化已有复盘并继续发现新结果，同时保持修订、可信度和实践发布各自的人工边界 | 需要多人分别确认、撤销单次确认或审计每批复验意见时，把单游标升级为有界验证事件日志，但仍不允许确认事件自动改写原则或发布实践资产 |
| IMP-052 | 2026-08-08 | 通过本地 stdio MCP 暴露唯一的只读事前原则核对工具；Bridge 只向带运行令牌的回环接口查询最多 3 条已采纳原则，不调用模型、不写决策、不建立原则采用关系，也不唤醒窗口 | 被动 Hook 只能在答案完成后观察，无法提供真正的选择前支持；独立只读工具可以保留客户端原生提问和故障放行，同时让适用条件、注意事项与匹配原因在选择前可见。安装只有在两个客户端 MCP 配置成功后才写入 v3 Hook 标记，避免界面误报完整接入 | 客户端提供强制的提交前阶段、可信调用身份或需要审计事前查询时，升级协议与显式记录边界；在此之前不把 Agent 是否调用或建议是否影响答案伪装成事实 |
| IMP-053 | 2026-08-08 | 事前核对质量观察只保存调用、命中、强弱匹配、来源与耗时的聚合计数和最近 30 个日期，不保存查询正文、项目、原则编号或逐次事件；本地试算复用同一匹配服务但不进入统计 | 完全不可观察会让客户端是否真正调用、匹配是否过宽都只能靠猜测，而保存逐次查询会扩大敏感决策上下文的留存范围；聚合覆盖指标和一次性试算可以支持校准，同时不把覆盖率伪装成准确率，指标写入也不阻塞 MCP 响应 | 需要准确率评估时，先设计用户明确触发、可撤销且不把推断写成事实的反馈契约；不能从命中率或后续答案反推建议正确性 |
| IMP-054 | 2026-08-08 | 方法论顶部把复盘素材、原则复验和关系核对合并为单一“工作箱”入口，工作箱只汇总数量和导航，三条队列继续复用原有独立操作与确认边界 | 状态筛选、多个维护队列和新建动作同时横排会在窗口缩小时形成视觉竞争，也让不同性质的待办看起来像同一级即时命令；集中入口降低工具栏密度，同时避免为同一事实再造第二套批处理接口 | 工作箱事项增长到需要排序、分派或跨设备同步时，再引入有游标的统一收件箱投影，仍不得把打开、关闭或导航视为事实处理 |
| IMP-055 | 2026-08-08 | 每次事前核对只签发短期内存匿名回执；只有用户明确评价后才按评价、来源和匹配强度累加聚合计数，回执消费即删且不保存正文、编号或逐次事件 | 覆盖率只能证明返回过候选，无法判断结果是否有帮助；持久化逐次查询或反馈又会扩大敏感决策上下文和可关联标识的留存。一次性回执可以去重并把明确反馈归入可校准桶，同时让客户端和本地试算复用同一契约 | 主动评价样本达到可分层评估规模且具备独立保留集后，再考虑阈值建议或语义召回实验；在此之前不自动调参，也不把自选择样本称为准确率 |
| IMP-056 | 2026-08-08 | 人工合并从两条扩展为单次 2 至 5 条已采纳原则；组内每一对都必须存在明确的“重复”人工事实，草案采纳时再次完整核验，来源归档与恢复按整组有界写入并在失败时回滚 | 成对入口适合发现重复，但重复知识常形成小型簇；只沿关系链传递会把不满足传递律的人工判断误当成事实。5 条上限把关系核对控制在最多 10 对，也让来源比较、证据并集、实践资产影响预览和恢复范围保持可读可测 | 真实重复簇长期超过 5 条或逐对确认成本出现可测量阻塞时，再设计显式关系组及其版本、拆组和冲突语义；不得用模型聚类结果直接替代人工关系事实 |
| IMP-057 | 2026-08-08 | 多来源合并编辑器内嵌缺失关系的逐对人工核对；候选至少要与组内一条原则已确认重复且不能已有冲突/无关，所有缺失对确认重复后才加入，任一阻断结论立即停止 | 要求用户退出合并、进入全局关系队列、再返回重建选择会丢失上下文并制造往返；直接放宽为关系链推断又会破坏事实边界。复用现有逐对关系写入并在同一弹窗切换到聚焦比较态，可以同时减少导航成本、避免堆叠弹窗并保持每次判断独立可撤销 | 候选规模让局部连接列表过长或需要协作分派时，引入有游标的关系任务投影；不得把未完成选择持久化为正式关系组或自动补齐关系 |
| IMP-058 | 2026-08-08 | 多来源合并编辑器把来源编号、未完成正文和所选证据保存为单份本机私有恢复草稿；关闭时先保存，恢复时重新校验来源状态、全量成对重复事实和同组现有候选，失效草稿保持惰性并只允许丢弃 | 合并最多包含 5 个来源、5 个正文域和 5 条证据，窗口关闭或 App 重启后要求用户重建选择会造成真实输入损失；把它写成正式候选又会绕过完整性检查。复用受限大小、仅当前用户可读的未完成表单存储，可以恢复编辑意图，同时让 Markdown 关系和原则继续作为权威事实 | 需要跨设备或多人同步草稿时，先设计端到端加密、过期策略和并发冲突界面；不得把私有草稿同步、恢复或自动保存视为关系确认、候选创建或原则采纳 |
| IMP-059 | 2026-08-08 | 原则修订编辑器保存单份带来源原则编号和基线更新时间的本机私有草稿；恢复前重新校验来源版本、采纳状态、现有修订候选与采用后新复盘，失效证据只从选择中移除 | 修订同时包含正文和采用后证据，丢失编辑代价高；但把旧编辑无条件恢复到已变化的正式原则会跨越版本边界。记录基线并以当前 Markdown 与复盘投影重验，可以保留输入又不覆盖外部修改或重复建立候选 | 需要在来源更新后迁移旧草稿时，先提供可见的逐字段三方比较与用户选择；不得自动重放旧正文、把失效复盘继续视为证据或绕过现有修订候选 |
| IMP-060 | 2026-08-08 | 新建原则修订草稿同时保存来源正文快照；来源变化后按字段执行三方比较，只有当前正式版本与草稿都偏离原基线的字段要求人工选择，确认后只重建当前版本上的私有草稿 | 单纯阻止恢复能避免覆盖，却会让用户丢弃已经完成的大量编辑；自动套用旧正文则可能抹掉正式原则的新边界。可见的基线、当前值和草稿值让安全修改确定性合并、真实冲突显式处理，同时继续在迁移前重验来源、候选和证据 | 需要跨设备同步或多人同时编辑修订草稿时，引入带作者、版本和过期策略的加密草稿协议；不得把字段预选视为正式内容确认，也不得自动建立或应用修订候选 |
| IMP-061 | 2026-08-13 | 用对现有 SQLite 快照的单遍本地聚合取代临时内存 DuckDB，废止 IMP-025 的运行时选择 | 当前五组统计只处理已经完整载入内存的决策数组，再导入第二个引擎没有新增能力；实测 `libduckdb.dylib` 占 117,005,056 字节、依赖目录约 114 MiB。确定性 O(n) 聚合保留全部指标、排序与恢复边界，同时显著缩小安装包和原生加载面 | 完整快照聚合出现可测量延迟或内存压力，且直接 SQLite 聚合经基准验证能改善真实数据规模时，把同一统计契约下沉为只读存储查询；不预先引入第二数据库 |
| IMP-062 | 2026-08-13 | macOS 打包显式分为本地 ad-hoc 与正式 Developer ID 两种模式；正式模式缺少硬化签名或完整公证凭据时失败，0.1 只发布带 SHA-256 清单的人工更新 | 强制所有开发构建持有 Apple 密钥会阻断日常验证，而把 ad-hoc ZIP 当作公开版本会要求用户绕过 Gatekeeper；自动更新在没有稳定 HTTPS feed、回滚与正式签名链路时又会扩大供应链风险。双模式可以保留本地效率并让发行失败关闭，版本化清单先固定未来更新输入而不引入后台网络 | 受保护 macOS runner 的 Developer ID、公证和制品上传持续通过，且更新源、回滚、用户提示和隐私边界完成集成验证后，再启用签名 App 的自动更新 |
| IMP-063 | 2026-08-13 | 正式发布先执行零容忍的运行时依赖审计；构建工具链告警单独登记并只做兼容范围内升级，不用跨主版本 override 强行清零 | 随 App 交付的依赖风险必须阻止发行，但注册表联网检查不应阻断离线日常开发；Electron Forge 上游范围内仍有不可直接修复的打包期告警，绕过其版本契约可能让签名、原生重建或制品完整性失效 | Forge、Packager 或 Rebuild 提供兼容修复后立即升级，并在受保护构建机重跑全量质量、冒烟、签名、公证和制品校验；在此之前只处理可信输入和官方制品 |
| IMP-064 | 2026-08-13 | 候选审核的确认/忽略继续只表示工作流动作，不作为分类正确/错误标签；产品质量只使用用户明确触发的匿名聚合评价，语义激活仍以 500 条分层脱敏标签集和独立保留集为准 | 忽略可能代表“正确但不重要”，确认也可能发生在用户编辑和补理由之后；把二者直接统计为准确率既有自选择偏差，也会在没有新同意边界时扩大行为留存。现有一次性匿名回执能发现明显阈值问题，但不能替代可复现评测 | 确实需要候选级标签时，另行设计明确、可撤销、限期且不保存正文的标注契约；达到分层样本规模并通过未参与调参的保留集前，不自动调阈值或扩大自动化 |
| IMP-065 | 2026-08-15 | 组织品牌采用 `Cognelis`，当前产品继续使用 `Decision`；代码包使用 `@cognelis/decision-*`，GitHub 发布到 `cognelis/decision` | 组织名需要容纳未来多个知识系统，产品名需要准确表达当前决策平台；产品前缀包名避免组织内未来出现宽泛 `core` 等命名冲突 | 组织定位或产品边界发生实质变化 |
| IMP-066 | 2026-08-15 | 品牌迁移以“历史沉淀可用、待处理可用”为两条硬性验收标准；旧名称只保留在集中式兼容模块、入口、测试和迁移说明中，兼容覆盖整个 `1.x` | Markdown 历史与尚未落盘的 spool/草稿同样构成用户资产；只做文本替换或只依赖索引重建都会造成真实数据或未完成工作丢失 | `2.0` 规划移除旧兼容入口前，先基于真实升级分布重新审计 |
| IMP-067 | 2026-08-15 | 默认应用数据目录在打开任何存储前从旧名称原子迁移到 `Decision`；既有 Obsidian vault 路径不自动移动，新旧目录并存时不猜测性合并 | 同父目录原子移动可以一起保存模型、凭据、索引和待处理状态且不产生大型副本；vault 是用户拥有并被 Obsidian 引用的内容位置，自动改名风险更高 | 需要跨卷迁移、自动合并两个活动目录或用户明确请求移动 vault |
| IMP-068 | 2026-08-15 | GitHub 使用干净根提交发布公开 `cognelis/decision`，在同一提交创建 MIT 授权的 `v1.0.0` 源码 Release，不上传 ad-hoc 二进制 | 现有历史含过时个人路径与作者信息；源码版本可以立即开源，而未签名 macOS 包不应诱导用户绕过 Gatekeeper | 正式 Developer ID 签名、公证和受保护制品上传链路持续通过 |
| IMP-069 | 2026-08-15 | 用户授权普通实施决策由 Codex 直接选择最优实现并记录，仅在权限、付费、不可逆数据操作、公开范围或核心验收冲突时请求决定 | 减少低价值打断，同时把真正改变权限和用户资产边界的事项保留给用户 | 用户撤回授权或项目进入多人治理阶段 |
| IMP-070 | 2026-08-15 | 新写入的 Markdown 使用当前内部标记；旧标记由单一兼容模块在读取和用户触发更新时规范化，用户正文中的同名文本保持原样 | 消除新文件中的旧品牌，同时保证历史决策、方法论和关系事实可继续读取与编辑，且不把品牌迁移扩大为用户内容改写 | `2.0` 移除兼容前，确认所有受支持的 1.x 笔记已完成升级验证 |
